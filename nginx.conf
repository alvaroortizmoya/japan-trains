server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;
    charset utf-8;

    location ~* \.js$ {
        add_header Content-Type application/javascript;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}